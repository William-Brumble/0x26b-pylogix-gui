const TagsContainer = () => {
    return <TagsPresentation />;
};

const TagsPresentation = () => {
    return (
        <div>
            <div>TAGS</div>
        </div>
    );
};

export { TagsContainer as Tags };
