/*
import type { Meta, StoryObj } from "@storybook/react";

import { Tags } from "@/pages/ManualOperation.tsx";

const meta = {
    title: "pages/tags",
    component: Tags,
} satisfies Meta<typeof Tags>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {
    args: {},
};
*/
